snapshots:
    - name: dim_listings
      relation: ref('listings') 
      config:
          unique_key: LISTINGS_LISTING_ID
          schema: gold
          database: AIRBNB
          strategy: timestamp
          updated_at: Listings_created_at
          dbt_valid_to_current: "to_date('9999-12-31')"