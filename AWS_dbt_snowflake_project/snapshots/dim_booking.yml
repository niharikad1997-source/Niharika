snapshots:
    - name: dim_bookings
      relation: ref('bookings') 
      config:
          unique_key: BOOKING_ID
          schema: gold
          database: AIRBNB
          strategy: timestamp
          updated_at: BOOKINGS_CREATED_AT
          dbt_valid_to_current: "to_date('9999-12-31')"